# Hardware Stage H9 Full-layer Results

Status: full-layer interleaved RTL entry implemented; not executed in the
current environment.

Implemented entry:

```text
make hw-h9-full-layer-test
bash scripts/sim/run_hw_h9_vcs.sh
```

The H9 RTL script now compiles the Stage 7D full `transformer_layer` bit-exact
testbench with:

```text
ATTENTION_PE_ARCH=PAPER_ARRAY
ATTENTION_SCHEDULE=INTERLEAVED
```

Configured runs:

| Run | N_HEAD | D_HEAD | Tokens | Coverage |
|---|---:|---:|---:|---|
| h9_layer_h1_d8 | 1 | 8 | 1 | full layer H1/D8 |
| h9_layer_h2_d8 | 2 | 8 | 1 | full layer H2/D8 |
| h9_layer_h2_d8_two_token | 2 | 8 | 2 | two-token KV growth through full layer |
| h9_layer_h4_d8 | 4 | 8 | 1 | full layer H4/D8 |
| h9_layer_h2_d16 | 2 | 16 | 1 | full layer H2/D16 |

The testbench checks the existing full order:

```text
RMSNorm1 -> Q/K/V projection -> interleaved Attention -> W_O -> residual1
-> RMSNorm2 -> W1 -> ReLU -> W2 -> residual2 -> final output -> layer done
```

Expected outputs are generated by the Stage 7D full-layer reference and are
compared bit-for-bit in the RTL testbench.

Current execution result:

```text
vcs: NOT FOUND
result=FAIL
```

Because VCS is unavailable in this environment, these full-layer runs are not
counted as passing HW-H9 acceptance.
